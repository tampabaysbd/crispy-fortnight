
@{
    ViewBag.Title = "Contact Manager";
}

<div>
    <div class="jumbotron">
        <h1>Contact Manager</h1>
    </div>
    <div>
        @Html.ActionLink("Add a Contact", "Add", "Contact", null, new { @class = "btn btn-primary" })
        @Html.ActionLink("View Contact List", "Index", "Contact", null, new { @class = "btn btn-primary" })
    </div>
</div>
